'use strict';
// Const assert = require('yeoman-assert');
// const helpers = require('yeoman-test');

describe('android-lib:linters placeholder', () => {
  it('installs checkstyle');
  it('installs PMD');
  it('installs FindBugs');
});
